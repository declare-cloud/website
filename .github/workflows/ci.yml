name: CI/CD

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
    paths:
      - '**.ts'
      - '**.tsx'
      - '**.js'
      - 'package.json'
      - 'bun.lock'
      - 'Dockerfile'
      - 'chart/**'
  pull_request:
    paths:
      - '**.ts'
      - '**.tsx'
      - '**.js'
      - 'package.json'
      - 'bun.lock'
      - 'Dockerfile'
      - 'chart/**'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: declare-cloud/website

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-
      - run: bun install
      - run: bun run format:check
      - run: bun run lint:check

  unit-tests:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-
      - run: bun install
      - run: bun run test:coverage

  e2e-tests:
    runs-on: ubuntu-latest
    needs: unit-tests
    if: github.event_name == 'pull_request' || startsWith(github.ref, 'refs/heads/main')
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-
      - run: bun install
      - run: bunx playwright install --with-deps
      - run: bun run test:e2e -- --reporter=dot
      - name: Upload Playwright Report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

  build-and-push:
    runs-on: ubuntu-latest
    needs: e2e-tests
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-

      - run: bun install

      - name: Determine Versions
        id: get_version
        run: |
          SHORT_SHA=$(echo "${GITHUB_SHA}" | cut -c1-7)

          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            PR_NUMBER="${{ github.event.pull_request.number }}"
            CONTAINER_TAG="pr-${PR_NUMBER}.${SHORT_SHA}"
            EXTRA_TAG="pr-${PR_NUMBER}"
            HELM_VERSION="0.0.0-pr${PR_NUMBER}.${SHORT_SHA}"
            HELM_EXTRA_VERSION="0.0.0-pr${PR_NUMBER}"
          elif [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            TAG_VERSION="${GITHUB_REF#refs/tags/v}"
            CONTAINER_TAG="${TAG_VERSION}"
            EXTRA_TAG="latest"
            HELM_VERSION="${TAG_VERSION}"
            HELM_EXTRA_VERSION="${TAG_VERSION}"
          else
            CONTAINER_TAG="dev-${SHORT_SHA}"
            EXTRA_TAG="dev"
            HELM_VERSION="0.0.0-dev.${SHORT_SHA}"
            HELM_EXTRA_VERSION="0.0.0-dev"
          fi

          echo "CONTAINER_TAG=${CONTAINER_TAG}" >> $GITHUB_ENV
          echo "EXTRA_TAG=${EXTRA_TAG}" >> $GITHUB_ENV
          echo "HELM_VERSION=${HELM_VERSION}" >> $GITHUB_ENV
          echo "HELM_EXTRA_VERSION=${HELM_EXTRA_VERSION}" >> $GITHUB_ENV


      - run: bun run build

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          load: true
          push: false
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.CONTAINER_TAG }}

      - uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.CONTAINER_TAG }}
          format: 'table'
          exit-code: 1
          severity: 'CRITICAL,HIGH'
          ignore-unfixed: true
          hide-progress: true

      - name: Push Docker image
        if: success()
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.CONTAINER_TAG }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.HELM_EXTRA_VERSION }}

      - uses: azure/setup-helm@v4

      - name: Package Helm chart for main version
        run: |
          cp chart/Chart.yaml chart/Chart.yaml.bak
          sed -i "s/^version:.*/version: ${HELM_VERSION}/" chart/Chart.yaml
          sed -i "s/^appVersion:.*/appVersion: \"${CONTAINER_TAG}\"/" chart/Chart.yaml
          helm package chart --destination chart-out
          mv chart/Chart.yaml.bak chart/Chart.yaml

      - name: Package Helm chart for extra/dev tag
        run: |
          cp chart/Chart.yaml chart/Chart.yaml.bak
          sed -i "s/^version:.*/version: ${HELM_EXTRA_VERSION}/" chart/Chart.yaml
          sed -i "s/^appVersion:.*/appVersion: \"${CONTAINER_TAG}\"/" chart/Chart.yaml
          helm package chart --destination chart-out
          mv chart/Chart.yaml.bak chart/Chart.yaml

      - run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ${{ env.REGISTRY }} --username ${{ github.actor }} --password-stdin

      - name: Push Helm charts
        run: |
          for chart in chart-out/*.tgz; do
            helm push "$chart" oci://${{ env.REGISTRY }}/declare-cloud
          done
